# 02.3: Self-Test

No answers here on purpose, if I cannot answer these from memory I go back to [02.1](../02.1-exercise) and [02.2](../02.2-extra-practice) rather than looking them up.

1. From memory, write the one-line command to create and switch to a new branch called `test/example` in a single step.
2. Explain why Git refuses `git branch -d` on a branch with unmerged commits. What am I actually being protected from?
3. What is the difference between `git branch -r` and `git branch -a`?
4. If I delete a branch locally but not on GitHub, what will `git fetch --prune` do the next time I run it?
5. Name the alias from my dotfiles that starts a brand new branch off an up-to-date main in a single command.

## Pass criteria

I consider this module done when I can answer all five without opening [02.1](../02.1-exercise) or my dotfiles for a reminder.
