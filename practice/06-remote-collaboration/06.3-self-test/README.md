# 06.3: Self-Test

No answers here on purpose, if I cannot answer these from memory I go back to [06.1](../06.1-exercise) and [06.2](../06.2-extra-practice) rather than looking them up.

1. From memory, write the command to add a second remote called `upstream` pointing at a URL of my choosing.
2. What is a stacked pull request? Why would I use one instead of one large pull request?
3. Why does GitHub let me open a pull request from a fork's branch straight into the original repository's `main`?
4. What is the difference between fetching `upstream` and merging it, versus rebasing onto it?
5. Name the command that lets me check out someone else's pull request locally without cloning their fork by hand.

## Pass criteria

I consider this module done when I can answer all five without opening [06.1](../06.1-exercise) for a reminder.
