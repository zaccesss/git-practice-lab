# 08.3: Self-Test

No answers here on purpose, if I cannot answer these from memory I go back to [08.1](../08.1-exercise) and [08.2](../08.2-extra-practice) rather than looking them up.

1. What does `needs:` control between two jobs in the same workflow?
2. Why is pinning a third-party action to a commit SHA safer than pinning it to a tag?
3. What does a `concurrency` group with `cancel-in-progress: true` actually do to overlapping runs?
4. What is the difference between a workflow artifact and a workflow secret?
5. Explain, from memory, what `permissions: contents: read` at the top of a workflow restricts and why every workflow in my other repositories sets it explicitly.

## Pass criteria

I consider this module done when I can answer all five without opening [08.1](../08.1-exercise) or any of my other repositories' real workflow files for a reminder.
