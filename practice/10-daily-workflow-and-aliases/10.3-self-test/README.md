# 10.3: Self-Test

No answers here on purpose, if I cannot answer these from memory I go back to [10.1](../10.1-exercise) and [10.2](../10.2-extra-practice) rather than looking them up.

1. From memory, name the alias that stages, commits and pushes in one step. What does it expect as its argument?
2. What does `automerge` actually do that a plain `gh pr merge --squash --delete-branch` does not?
3. Name the full end-of-task alias that returns me to a clean, up-to-date `main` with a branch removed and everything pruned.
4. Why does `ghaddlabel` try a PR edit before falling back to an issue edit, rather than the other way round?
5. What is the current, real gap in my own alias set that I identified in this module? Why does it belong in `dotfiles` rather than here?

## Pass criteria

I consider this module done when I can answer all five without opening [10.1](../10.1-exercise) or my dotfiles for a reminder.
